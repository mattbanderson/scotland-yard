{
  "name": "scotland-yard",
  "version": "1.0.0",
  "description": "A web map for the Scotland Yard board game.",
  "main": "server.js",
  "scripts": {
    "prestart": "cd data/scripts && node generate-routes.js && cd ../..",
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Matt Anderson",
  "license": "MIT",
  "dependencies": {
    "mapbox-gl": "^0.39.1",
    "node-static": "^0.7.11"
  }
}
